# Signals Agent Backend Environment Configuration
# Copy this file to .env and update with your values

# AI Functionality (Optional)
# Set this to enable AI-powered ranking and proposals
# If not set, the system will return deterministic results
GEMINI_API_KEY=your_gemini_api_key_here

# Database Configuration (Optional)
# Override the default SQLite database path
DATABASE_PATH=./signals.db

# CORS Configuration (Optional)
# Comma-separated list of allowed origins for frontend access
# Default allows all origins (*)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.vercel.app

# Server Configuration (Optional)
# These are typically not needed for local development
# HOST=localhost
# PORT=8000

# Logging Level (Optional)
# LOG_LEVEL=INFO
